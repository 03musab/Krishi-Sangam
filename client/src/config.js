/* ═══════════════════════════════════════════
   Krishi Sangam — feature flags
   Flip a flag to `true` to enable the feature,
   then rebuild the client (npm run build).
   ═══════════════════════════════════════════ */

export const FEATURES = {
  // Browse-by-category equipment catalogue on the Equipment Rental page
  // (code is fully implemented — just hidden until this is enabled)
  equipmentCatalogue: false
};
