{
  "name": "krishi-sangam",
  "version": "1.0.0",
  "private": true,
  "description": "Krishi Sangam — Connecting Farmers, Land & Equipment",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && npm run build",
    "start": "cd server && node server.js",
    "dev": "cd server && node server.js"
  }
}
